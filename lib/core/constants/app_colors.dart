import 'package:flutter/material.dart';

/// All colors used across the Move Smart app.
/// Change a color here and it updates everywhere automatically.
class AppColors {
  AppColors._();

  static const Color primary       = Color(0xFF1565C0); // Deep blue
  static const Color primaryLight  = Color(0xFF1E88E5); // Lighter blue
  static const Color white         = Color(0xFFFFFFFF);
  static const Color background    = Color(0xFFF0F4FF); // Light blue-grey
  static const Color cardBg        = Color(0xFFFFFFFF);
  static const Color textDark      = Color(0xFF1A1A2E);
  static const Color textGrey      = Color(0xFF9E9E9E);
  static const Color divider       = Color(0xFFE0E0E0);
  static const Color error         = Color(0xFFD32F2F);
  static const Color success       = Color(0xFF388E3C);
}
